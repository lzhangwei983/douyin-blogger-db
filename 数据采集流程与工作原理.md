# 抖音博主数据库 — 数据采集流程与工作原理

> 本文说明：一条视频从「链接」变成「完整数据」的整个过程、点赞/播放数据是怎么拿到的、为什么手动粘贴字幕和分析会缺数据，以及下次导入该怎么做。

---

## 一、一次「链接 → 完整数据」的完整流程

一条视频的完整数据 = **文本内容**（字幕稿 + 分析）+ **平台数据**（标题、日期、时长、点赞、评论、转发、收藏）。

```
你发链接（一个或一批）
        │
        ▼
① 下载   yt-dlp 下载音视频 + 同时拿到平台元数据 JSON（点赞/评论/转发/收藏/标题/日期/时长）
        │
        ▼
② 转录   本地语音识别（ASR）把音轨转成字幕稿 txt（带分句）
        │
        ▼
③ 分析   LLM 阅读字幕稿 → 生成 7 段分析（摘要/核心观点/建议/行业方向/风险/可信度/值得执行）
        │
        ▼
④ 入库   import_leong.py 把以上全部写入 SQLite 三张表（bloggers / videos / analyses）
        │
        ▼
⑤ 查看   打开 App（exe）或浏览器即见完整数据
```

---

## 二、点赞/播放这些数据是怎么爬来的（工作原理）

### 数据源头：平台公开接口
抖音的每条视频都有唯一的 **video_id**（链接里 `https://www.douyin.com/video/7xxxxxxxx` 那段数字）。抖音网页版有公开的数据接口，传入 video_id 会返回该视频的完整元数据 JSON，其中就包含：

| 接口字段 | 含义 | 数据库字段 |
|---|---|---|
| `digg_count` | 点赞数 | like_count |
| `comment_count` | 评论数 | comment_count |
| `share_count` | 转发数 | repost_count |
| `collect_count` | 收藏数 | save_count |
| `play_count` | 播放量 | （当前表未存，可加） |
| `desc` | 标题/文案 | title |
| `create_time` | 发布时间 | upload_date |
| `duration` | 时长（毫秒） | duration |
| `aweme_id` | 视频ID | video_id |

### 抓取工具：yt-dlp
本项目用 **yt-dlp**（开源命令行下载器）作为抓取引擎。它的原理是内置了上百个平台的「提取器（extractor）」，每个提取器模拟对应平台的网页/客户端请求：

```
yt-dlp https://www.douyin.com/video/7xxx --write-info-json
        │
        ▼
抖音提取器 → 模拟浏览器请求公开接口 → 拿到：
  ① 视频流地址（用于下载音轨）
  ② 完整元数据 JSON（点赞/评论/转发/收藏/标题/日期/时长…）
```

一次命令同时拿到「下载所需地址」和「互动数据」，不需要自己写抓包脚本，也不依赖任何第三方数据服务。同一个工具支持 B站、YouTube、TikTok、快手等平台——这就是概览页「平台分区」能扩展多平台的基础。

### 为什么手动粘贴字幕和分析会缺点赞数据？
因为两类数据来自完全不同的渠道：

| 数据 | 来源 | 能否手动粘贴 |
|---|---|---|
| 字幕稿 | 本地语音识别（需要视频文件） | 能，但繁琐 |
| 7 段分析 | LLM 阅读字幕生成 | 能 |
| **点赞/评论/转发/收藏/日期/时长** | **平台接口 JSON（抓取）** | **不能——人工手里没有这些数字** |

数据库里 `like_count` 等字段只能由抓取脚本自动填充。手动粘贴文本内容时这些数字字段就是空的（显示 —）。所以**正确的导入姿势是发链接给我**，让流程①在下载时自动把互动数据一并取回，而不是只粘贴文本。

### 风控说明
抖音对高频请求有风控（验证码、限流）。目前只采集**公开数据**、低频请求、本地使用，不涉及任何非公开信息。批量大规模采集存在账号风险，因此本项目保持「低频 + 公开接口 + 本地存储」的克制路线。

---

## 三、字幕提取原理（语音识别）

```
音轨（来自①下载） → 本地 ASR 模型 → 分句文本 + 时间戳 → 字幕 txt
```

- ASR 在**本地**运行（不需要上传任何内容到第三方），支持中文口语、多说话人混音
- 特殊情况：
  - **无语音视频**（只有 BGM、无人声）→ 识别结果为空 → 标记 `无语音`（status=empty）
  - **图文**（没有视频、没有音频）→ 直接标记 `图文`（status=skip），不转录

---

## 四、分析生成原理

字幕稿是「原料」，LLM（大语言模型）按固定模板产出结构化分析：

```
字幕全文 → LLM → 7 段分析：
  内容摘要 / 核心观点 / 可执行建议
  适合的行业或工作方向（归纳博主建议，非事实）
  风险和可疑之处 / 对博主可信度的判断 / 哪些建议值得执行、需谨慎
```

每段分析保存为 md 文件，入库时按 `## 小节标题` 切分，分别存入 analyses 表的 7 个字段——App 里「分析」面板的 7 个 tab 就是直接读这些字段。

---

## 五、入库原理

`import_leong.py` 读取 4 个数据源，组装成完整记录：

| 数据源 | 内容 | 用途 |
|---|---|---|
| `作品链接.csv` | 序号/类型/链接（87 条权威顺序） | 遍历骨架、seq、kind、video_id |
| `_progress.json` | 每条的状态（ok/empty/skip）+ 字幕文件 | status、字幕内容 |
| `_meta.json` | 每条的平台元数据 | 标题/日期/时长/互动四件套 |
| `*_analysis.md` | 每条的 7 段分析 | analyses 表 |

```
csv ──┐
progress ──┤──→ import_leong.py ──→ SQLite
meta ──┤      （douyin_blog.db）
analysis.md ─┘     ├── bloggers   （1 条）
                   ├── videos     （87 条：字幕+互动+状态）
                   └── analyses   （68 篇：7 段分析）
```

---

## 六、下次导入的正确姿势（你只需要做一步）

```
你：把链接发给我（粘贴 / txt / CSV 都行）
我：① 下载+取互动数据 → ② 转录字幕 → ③ LLM 分析 → ④ import_leong.py 入库 → ⑤ 覆盖 App 的 douyin_blog.db
你：打开 App 即可看到完整数据（含点赞/评论/转发/收藏）
```

> 注意：如果你在 App 里手动编辑/新增过记录，先告诉我一声——我会改成「合并导入」（按 video_id 去重，保留你在 App 里改过的东西），而不是整库覆盖。

---

## 七、扩展：其他平台

同样的流水线适用于 B站 / YouTube / TikTok / 快手：
1. 新建博主时填平台名（抖音/B站/YouTube…），App 概览页「平台分区」自动统计
2. yt-dlp 原生支持这些平台的提取器，采集原理完全相同
3. 各平台字段略有差异（如 B站用 coin_count 硬币），入库脚本做字段映射即可